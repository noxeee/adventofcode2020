import scala.io.Source

// reading file in Scala
val filename = "input_day8.txt"
val lines = Source.fromFile(filename).getLines.toArray

def read(input:Array[String]){
    
    
    input match {
        case "nop" <= break
        case "acc" <= 
        case "jmp" =>
    }
}