getwd()

input <-unlist(read.table("input.txt"))

pairs <- combn(c(input), 2, {function(x,y) (x+y) == 2020 (x,y)})

pairs <- combn(c(input), 2, {function(x,y) (x+y) == 2020 (x,y)})

function(pair1,pair2) {
    if((pair1 + pair2 == 2020)) {
        return pair1, pair2
    }
}





for (pair in 0:size(pairs)) stopif(pairs[] + pairs[] == 2020)

input <- read.table("input.txt")

pairs <- combn(input)



for (pair in 0:size(pairs)) stopif(pairs[] + pairs[] == 2020)



#https://www.cplusplus.com/reference/algorithm/